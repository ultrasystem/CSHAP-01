***************
*** 1198,1204 ****
  	if (copied && likely(!tp->repair))
  		tcp_push(sk, flags, mss_now, tp->nonagle);
  	release_sock(sk);
- 	return copied;
  
  do_fault:
  	if (!skb->len) {
--- 1256,1262 ----
  	if (copied && likely(!tp->repair))
  		tcp_push(sk, flags, mss_now, tp->nonagle);
  	release_sock(sk);
+ 	return copied + copied_syn;
  
  do_fault:
  	if (!skb->len) {
